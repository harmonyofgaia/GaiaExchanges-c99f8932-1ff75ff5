[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.18.0"
  HUSKY = "0"
  CI = "true"

# Single Page App fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200