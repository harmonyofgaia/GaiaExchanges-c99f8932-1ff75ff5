[build]
  command = "NODE_ENV=production npm install --legacy-peer-deps && npm run build"
  publish = "dist"

# Single Page App fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200